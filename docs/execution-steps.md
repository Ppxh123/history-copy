# 开发执行步骤

> 每个阶段完成后，在 `dev-logs/` 目录下记录完成情况和待办事项。

## 阶段 1：项目骨架

**目标**：搭建可运行的基础 Electron + React 项目

| 步骤 | 内容 | 验证标准 |
|------|------|---------|
| 1.1 | 初始化 package.json，安装 Electron + Vite + React + TS 依赖 | `npm install` 无报错 |
| 1.2 | 配置 vite.config.ts、tsconfig.json、electron-builder.yml | 文件存在，配置合理 |
| 1.3 | 创建 electron/main.ts（最小窗口） + src/main.tsx（Hello World） | — |
| 1.4 | npm run dev 能打开 Electron 窗口，显示 React 内容 | 窗口正常显示 |

## 阶段 2：数据层

**目标**：实现 SQLite 数据库模块，完成 CRUD 操作

| 步骤 | 内容 | 验证标准 |
|------|------|---------|
| 2.1 | 创建 electron/database.ts，初始化数据库和建表 | 启动时自动创建 .db 文件 |
| 2.2 | 实现 query/add/delete/update/pin/getByHash/getCount 方法 | — |
| 2.3 | 实现 content_hash 计算（crypto MD5），去重检查 | 相同内容不重复入库 |
| 2.4 | 写一个简单测试脚本验证数据库读写正确性 | 增删改查无误 |

## 阶段 3：剪贴板监听

**目标**：实现文字和图片剪贴板的自动监听和存储

| 步骤 | 内容 | 验证标准 |
|------|------|---------|
| 3.1 | 创建 electron/clipboard-monitor.ts，500ms 轮询文字 | 复制文字后日志有输出 |
| 3.2 | 实现图片监听：读取 clipboard NativeImage，保存为 PNG | 截图后 images/ 下有文件 |
| 3.3 | 监听器检测到新内容 → 调用 database.add() 入库 | 数据库中有记录 |
| 3.4 | 同内容不重复记录（哈希去重） | 复制相同文字不新增 |

## 阶段 4：IPC 通信层

**目标**：建立主进程与渲染进程之间的安全通信

| 步骤 | 内容 | 验证标准 |
|------|------|---------|
| 4.1 | 定义 IPC channel 常量（get-items, delete-item, pin-item, copy-item, get-settings, update-settings） | — |
| 4.2 | 实现 electron/preload.ts，contextBridge 暴露安全 API | — |
| 4.3 | 主进程注册 ipcMain.handle 处理各 channel | — |

## 阶段 5：React UI 基础

**目标**：完成主要 UI 组件

| 步骤 | 内容 | 验证标准 |
|------|------|---------|
| 5.1 | 配置 Tailwind CSS，创建 src/index.css | Tailwind 类名生效 |
| 5.2 | 创建 App.tsx 布局（标题栏 + 主体区域） | 布局正确 |
| 5.3 | 创建 HistoryCard.tsx（文字 + 图片两种展示） | 卡片渲染正确 |
| 5.4 | 创建 HistoryList.tsx，从 IPC 获取数据渲染列表 | 能看到数据 |
| 5.5 | 实现空状态提示 | 无数据时显示提示 |

## 阶段 6：核心交互

**目标**：实现粘贴、删除、置顶、搜索

| 步骤 | 内容 | 验证标准 |
|------|------|---------|
| 6.1 | 点击卡片 → IPC 通知主进程恢复剪贴板 → 窗口隐藏 | 能粘贴到记事本 |
| 6.2 | 删除按钮 → 确认 → 删除数据库记录和图片文件 | 记录消失 |
| 6.3 | 置顶按钮 → 切换 pinned 状态 → 列表刷新排序 | 置顶项排最前 |
| 6.4 | 创建 SearchBar.tsx，实时过滤文字内容 | 搜索结果正确 |
| 6.5 | 搜索为空时显示「无匹配结果」 | — |

## 阶段 7：系统托盘与窗口行为

**目标**：实现系统托盘和窗口交互

| 步骤 | 内容 | 验证标准 |
|------|------|---------|
| 7.1 | 创建 electron/tray.ts，托盘图标 + 右键菜单 | 托盘有图标 |
| 7.2 | 左键托盘图标 → 呼出/隐藏窗口 | 窗口切换正确 |
| 7.3 | 关闭窗口 → 隐藏到托盘（不退出） | 关闭不退出 |
| 7.4 | 窗口定位在托盘图标附近 | 位置合理 |
| 7.5 | 窗口失焦自动隐藏 | 点其他地方窗口消失 |
| 7.6 | 窗口始终置顶 | 不被其他窗口遮挡 |
| 7.7 | 自定义无边框窗口 + 圆角 | 外观正确 |

## 阶段 8：设置与清理

**目标**：实现设置面板和自动清理机制

| 步骤 | 内容 | 验证标准 |
|------|------|---------|
| 8.1 | 创建 SettingsPanel.tsx（侧边滑出面板） | 面板正确展示 |
| 8.2 | 保留天数选择（1/3/5 天），存入 settings 表 | 设置可保存读取 |
| 8.3 | 实现自动清理：清理过期记录 + 删除对应图片文件 | 过期记录被清除 |
| 8.4 | 实现 500 条上限：超出时删除最旧非置顶记录 | 数量不超限 |
| 8.5 | 创建 electron/auto-start.ts，实现开关机自启 | 重启后托盘出现 |

## 阶段 9：UI 美化

**目标**：应用淡蓝色主题，打磨细节

| 步骤 | 内容 | 验证标准 |
|------|------|---------|
| 9.1 | 按设计规范应用所有颜色 | 视觉符合规范 |
| 9.2 | 添加卡片 hover 过渡动画 | 过渡流畅 |
| 9.3 | 自定义滚动条样式 | 滚动条美观 |
| 9.4 | 添加 SVG 图标 | 图标显示正确 |
| 9.5 | 整体视觉审查调整 | 简洁美观 |

## 阶段 10：打包与测试

**目标**：生成可安装的 Windows 应用

| 步骤 | 内容 | 验证标准 |
|------|------|---------|
| 10.1 | 配置 electron-builder.yml（appId、图标、NSIS 安装器） | 配置正确 |
| 10.2 | 执行 npm run build，生成 .exe 安装包 | 打包无报错 |
| 10.3 | 在 Windows 11 安装测试：安装 → 启动 → 托盘 → 复制 → 粘贴 | 全流程正常 |
| 10.4 | 测试开机自启 | 重启后自动运行 |
| 10.5 | 修复测试中发现的问题 | 所有功能正常 |
